// ** React Imports
const LayoutWrapper = props => {
    // ** Props
    const { children } = props

    return (
        <div>
            {children}

        </div>

    )
}

export default LayoutWrapper
